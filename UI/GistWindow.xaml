<Window x:Class="GuvenlikDuvarim.UI.GistWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="â˜ï¸ GitHub Gist Bulut Yedekleme"
        Height="490" Width="580"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Border Padding="20" Background="{DynamicResource WindowBackgroundBrush}">
        <StackPanel>
            <TextBlock x:Name="txtGistTitle" Text="â˜ï¸ GitHub Gist Bulut Yedekleme" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource PrimaryBrush}" Margin="0,0,0,10"/>
            
            <Border Background="{DynamicResource CardBackgroundBrush}" CornerRadius="6" Padding="10" Margin="0,0,0,12" BorderBrush="{DynamicResource SubtleBorderBrush}" BorderThickness="1">
                <TextBlock x:Name="txtGistHowToUse" Text="ğŸ’¡ NasÄ±l KullanÄ±lÄ±r?\nâ€¢ Gist yÃ¼klemesi yapmak iÃ§in 1 defalÄ±k GitHub Token almanÄ±z gerekir. (Token bilgisayarÄ±nÄ±zda kalÄ±cÄ± saklanÄ±r).\nâ€¢ YÃ¼kle butonuna bastÄ±ÄŸÄ±nÄ±zda profilleriniz Gist'e aktarÄ±lÄ±r ve var olan Gist gÃ¼ncellenir.\nâ€¢ BaÅŸka bir bilgisayarda ayarlarÄ± Ã§ekmek iÃ§in o Gist ID'sini yapÄ±ÅŸtÄ±rÄ±p Ä°ndir butonuna basÄ±n." 
                           FontSize="11" Foreground="{DynamicResource MutedForegroundBrush}" TextWrapping="Wrap" LineHeight="16"/>
            </Border>

            <DockPanel Margin="0,0,0,4">
                <TextBlock x:Name="lblPatToken" Text="GitHub Personal Access Token (PAT):" FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <TextBlock HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Hyperlink Click="HyperlinkGetToken_Click" ToolTip="1 saniyede Gist yetkili Token oluÅŸturma sayfasÄ±nÄ± aÃ§ar">
                        <Run x:Name="runGetTokenLink" Text="ğŸ”‘ 1-TÄ±kla Token Al (GitHub)"/>
                    </Hyperlink>
                </TextBlock>
            </DockPanel>
            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <PasswordBox x:Name="pbToken" Grid.Column="0" Padding="6,5" ToolTip="Örn: ghp_xxxxxxxxxxxxxxxxxxxx" PasswordChanged="PbToken_PasswordChanged"/>
                <Button x:Name="btnClearToken" Grid.Column="1" Content="🗑️ Tokenı Sil" Style="{DynamicResource DangerButton}" Margin="6,0,0,0" Padding="8,4" Click="BtnClearToken_Click" ToolTip="Kayıtlı GitHub Token bilgisini siler"/>
            </Grid>

            <TextBlock x:Name="lblGistId" Text="Gist ID veya Gist URL (Geri YÃ¼klemek Ä°Ã§in):" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <TextBox x:Name="txtGistId" Margin="0,0,0,10" Padding="6,5" ToolTip="Ã–rn: 8c01a517abdc43d5b3bf12d566a2cf6e"/>

            <CheckBox x:Name="chkAutoGist" Content="ğŸš€ Uygulama aÃ§Ä±lÄ±ÅŸÄ±nda otomatik Gist'e yedekle" Margin="0,0,0,14" FontSize="11" FontWeight="SemiBold" Checked="AutoGist_Changed" Unchecked="AutoGist_Changed"/>

            <UniformGrid Columns="3" Margin="0,0,0,12">
                <Button x:Name="btnUpload" Content="ğŸ“¤ Gist'e YÃ¼kle" Style="{DynamicResource SuccessButton}" Height="36" Margin="0,0,4,0" Click="BtnUpload_Click"/>
                <Button x:Name="btnDownload" Content="ğŸ“¥ Gist'ten Ä°ndir" Style="{DynamicResource SecondaryButton}" Height="36" Margin="2,0,2,0" Click="BtnDownload_Click"/>
                <Button x:Name="btnOpenGistUrl" Content="ğŸŒ Gist'i AÃ§" Style="{DynamicResource SecondaryButton}" Height="36" Margin="4,0,0,0" Click="BtnOpenGistUrl_Click" ToolTip="TarayÄ±cÄ±da Gist sayfasÄ±nÄ± aÃ§ar"/>
            </UniformGrid>

            <TextBlock x:Name="txtStatus" Text="" FontSize="11" Foreground="{DynamicResource SuccessBrush}" FontWeight="SemiBold" TextWrapping="Wrap" HorizontalAlignment="Center"/>
        </StackPanel>
    </Border>
</Window>
